// Package types contains the public node, XPath, and location data types used
// by xpath-go. Nodes are produced by the parser and returned through the
// top-level xpath package; callers should treat parser-owned pointer fields as
// read-only while traversing a result tree.
package types
